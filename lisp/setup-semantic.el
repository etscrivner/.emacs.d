(semantic-mode 1)

;; Highlight the function the current line belongs to
(global-semantic-highlight-func-mode 1)

;; Continue showing the top-level function we're inside at top of window
;;(global-semantic-stickyfunc-mode -1)

(provide 'setup-semantic)
